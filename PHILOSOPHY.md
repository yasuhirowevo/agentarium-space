# Agentarium Space — デザイン思想

[DESIGN.md](./DESIGN.md) が「何を作るか」の正典（仕様）であるのに対し、本書は
「**なぜそうなったか・何を良しとし、何を捨てたか**」の正典である。
このプロジェクトを引き継ぐ開発者・AI エージェントは、見た目や挙動に手を入れる前に本書を読み、
ここにある判断基準で自分の変更を検分すること。仕様の詳細は DESIGN.md の該当節（v2.x）へ。

## 1. プロダクトの本質 — これは「観賞するもの」

Agentarium Space は監視ダッシュボードではない。**水槽やプラネタリウムのように
「眺めて心地よい」ことが第一目的**の観賞装置であり、その上で
「誰が何をしているか」が一目で分かる実用性を備える。

北極星はただひとつ: **「AI が働くのを上から眺める楽しさ」**。

- 機能を足すときは「これは眺める楽しさを増すか？」を先に問う。情報として正しくても、画面をうるさく・目に痛くするなら不採用
- 逆に、美しくても「誰が何をしているか」を覆い隠す装飾も不採用（v2.1 の教訓）。**美観と可読性はトレードオフではなく両立必須**

## 2. 造形言語

世界観は「**夜の星図の下、発光する生き物たちの生態系**」。

- **二層構造**: 生き物レイヤ（orb = 丸い発光体・点の目・瞬き・愛嬌）と、計器レイヤ（ガラスモジュール・コーナーブラケット・走査線・スモールキャップス英字ラベル = 近未来管制室 FUI）。かわいさと近未来を混ぜず、**レイヤで分離して同居**させる
- **色は 4 系統のみ**: Claude = 暖色（コーラル→アンバー）/ Codex = 寒色（アイスシアン）/ テキスト = 白の不透明度階調（40〜85%）/ 警告 = 琥珀・切断 = 赤。彩度の高い UI 色をこれ以外に増やさない
- **光で語る**: 状態の違いは輝度・グロー・波紋・呼吸（低周波の膨縮）で表す。形の変形・振動・点滅・絵文字では表さない
- **背景の語彙は「星図」**: 天球グリッド・軌道弧・瞬く星・流れ星（v2.9）。レーダー・スイープなど軍事管制の語彙はプラネタリウム系世界観への移行時に廃止した

## 3. 反復で確立した原則（歴史と教訓）

いずれも実際のユーザーフィードバックから生まれた憲法である。見出しの版番号は DESIGN.md の節に対応する。

### 安定性 > 反応性（v2）

v1 は activity 順ソートでレイアウトが目まぐるしく組み替わり「目が疲れる」と却下された。
配置は**決定論**（プロジェクト key のアルファベット順 phyllotaxis・key シード乱数）とし、
並び替え・テレポートを禁止。すべての位置・輝度・半径は指数減衰 lerp の連続変化で動かす。

### 震え禁止（v2）

shake は「分かりやすいが見ていて目が疲れる」。稼働表現は波紋・輝度・呼吸へ。
**高周波の運動は全面禁止**。

### 既視感の拒否（v2）

「ありきたり」は明確な失敗判定。汎用ダッシュボードにも汎用バーチャルオフィスにも寄せない。
DOM カードを捨て、Canvas 1 枚に描く固有の世界を作った。
今後も「どこかで見た UI」に近づいたら危険信号と捉えること。

### 可読性は常時レイヤ（v2.1）

初版 Lumen Bay は美しいが「誰が何をやっているか重なりすぎて全然わからん」と却下された。
ネームプレート常時表示・ネームプレート込み楕円距離での衝突分離・親子関係線を常時レイヤとして足した。
**「hover しないと分からない」は敗北**。

### 中身を実況する（v2.2）

状態色だけでは仕事の中身が見えない。`ツール名: 対象`（例 `Edit: office.js`）の実況・
発話の吹き出し・イベントフィードで「いま何をしたか」を文字で見せる。
**実ログ由来の具体的な文字列こそが面白さの源泉**。抽象化しすぎない。

### 嘘の計器を作らない（v2.3 / v2.5）

近未来 FUI はダミー数値で埋めたくなるが、**実データ駆動でない計器・意味のない点滅は禁止**。
近似が破綻したら（例: コンテキスト使用率が近似窓を超える）嘘の 100% を出さず、
リングを消して生値 `CTX 523k` の表示にフォールバックする。誠実さ > 見栄え。

### 覆わない（v2.4）

オーバーレイ式パネルは下の orb を隠すから却下された。パネルは常設ドックとし、
**Canvas とパネルは領域を分け合い、重ねない**。

### 情報密度は善、ただし置き場所を選ぶ（v2.5 / v2.7）

「もっと色んな情報が常時画面上に」。クリックしないと見えない情報を減らす。
ただし密度は**パネル・HUD・ツリー側に厚く**盛り、**Canvas（orb 周り）はミニマルを守る**。
orb 周りへの追加は 1 変更につき最小限に絞る（v2.7 では状態行拡張と ▲ マーカーの 2 点のみ）。

### 構図は管制卓（v2.6 / v2.8）

一枚板の全高ドロワー → 浮かぶモジュール群 → 左右デュアルレールへと洗練した。
**中央 Canvas が主画面、左が情報レール（FOCUS / AGENT TREE）、右がログ（LIVE STREAM）**。
モジュールは上下 12px インセットで浮かぶウィジェットにし、画面いっぱいの板にしない。
同じ情報の二重表示は片方を消す（v2.8 で Canvas 内ティッカーを廃止）。

### 名は体を表す（v2.9）

名称・見た目・語彙は一致させる。Agentarium Space の名称とプラネタリウム系世界観に合わせ、
背景をレーダーから星図へ置き換えた。ズレに気づいたら放置しない。

### YAGNI は UI にも適用する（v2.9）

使われない ⇄ 左右切替は「ボタンを隠す」ではなく**設定・永続化ロジックごと撤去**した。
オプションを溜め込まない。

## 4. 不可侵の制約

DESIGN.md「原則」の再掲 + 実装上の骨格。変更には特段の理由と明示の合意が要る。

- **読み取り専用**: ログファイルへの書き込み・改変・削除ゼロ
- **完全ローカル**: 127.0.0.1 bind のみ。外部通信・telemetry ゼロ。HTTP Host / WS Origin 検証を外さない
- **商標配慮**: Anthropic / OpenAI の商標・ロゴ・アセット不使用。"Claude Code" / "Codex" はデータソース名としてのテキスト表記のみ
- **耐バージョン性**: 未知のレコード型・JSON.parse 失敗行で絶対にクラッシュしない（黙って無視）
- **パフォーマンス礼儀**: 30fps キャップ・`document.hidden` で描画完全停止・`prefers-reduced-motion` 対応。この 3 構造を壊す変更は不可
- **XSS 回避**: ログ由来文字列の描画は fillText / textContent のみ。innerHTML への挿入禁止

## 5. 再導入禁止リスト

過去に意図して捨てたもの。「改善」として再提案しないこと。

- 部屋・デスク・アバターのバーチャルオフィスメタファ（v1 で全廃）
- activity 順ソートによるレイアウト再配置
- shake・振動・高周波アニメーション、絵文字ステータス
- オーバーレイ・スクリム式のパネル（下の要素を覆う UI 全般）
- 回転レーダースイープ・放射スポークの背景
- ⇄ パネル左右切替ボタンと ×（選択解除）ボタンのツールレール
- Canvas 内右下のアクティビティティッカー（右レール LIVE STREAM と重複）
- ダミーデータの計器・装飾のためだけの点滅

## 6. 用語集

| 用語 | 意味 |
|---|---|
| Lumen Bay | UI v2 コンセプトのコードネーム |
| Pool / SECTOR | プロジェクト単位の発光リング |
| Orb | セッション個体 |
| Satellite | Codex 子セッション（親を公転） |
| Spark | Claude subAgent の周回光点 |
| ネームプレート | orb 下の常時ラベル |
| HUD | 全幅ヘッダの計器列 |
| FOCUS | 左レールの計器モジュール |
| AGENT TREE | 左レールの階層ツリー |
| LIVE STREAM | 右レールのイベントログ |

## 7. 拡張の作法

1. **仕様が先**: 変更はまず DESIGN.md に versioned 節（v2.x）として追記し、それを正典として実装する。本書には仕様ではなく、新たに確立した判断基準・捨てた選択肢を追記する
2. **既存レイヤの削除禁止**: v2.1 以降の各レイヤは積層である。再配置は可、機能削除には明示の合意が要る
3. **セルフチェック** — 新しい表現を足す前に全問 yes であること:
   - 実データ駆動か（ダミーなし・null 安全か）
   - 決定論的か（key シードで、ロードごとに配置が変わらないか）
   - 連続変化か（lerp 遷移で、テレポート・カット切替がないか）
   - 低周波か（震え・高速点滅がないか）
   - reduced-motion / 30fps キャップ / visibility 停止の既存構造に乗っているか
   - Canvas をクラッタ化していないか（密度はパネル・HUD 側へ）
   - 他の要素を覆っていないか
4. **検証**: `pnpm run scan`（状態 JSON）→ `pnpm run web`（stdout に表示された起動ごとの URL をブラウザで開く）→ 実描画の確認（headless 検収には Electron の capturePage が使える）。実ログでの目視確認を省略しない
